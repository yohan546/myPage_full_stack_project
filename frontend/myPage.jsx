import React from "react";
import ReactDom from "react-dom";

document.addEventListener(("DOMContentLoaded"), () => {

    const root = document.getElementById("root")

    ReactDom.render(<h1>Welcome to myPage!</h1>, root)
});
